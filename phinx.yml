paths:
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: phinxlog
    default_database: test
    production:
        adapter: mysql
        host: localhost
        name: coreframework_db
        user: root
        pass: ''
        port: 3306
        charset: utf8

    development:
        adapter: mysql
        host: localhost
        name: coreframework_db
        user: root
        pass: 'qwedsa'
        port: 3306
        charset: utf8

    testing:
        adapter: mysql
        host: localhost
        name: test
        user: root
        pass: 'qwedsa'
        port: 3306
        charset: utf8
