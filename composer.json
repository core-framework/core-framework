{
    "name": "core-framework/core-framework",
    "description": "Core PHP Framework",
    "type": "project",
    "license": "MIT",
    "keywords": [
        "php",
        "framework",
        "Core",
        "Core PHP Framework"
    ],
    "prefer-stable": false,
    "autoload": {
        "psr-4": {
            "Core\\": "../core/src/Core",
            "app\\": "/app"
        }
    },
    "require": {
        "php": ">=5.4.0",
        "core-framework/core": "^v0.0.5-alpha"
    },
    "repositories": [
        {
            "type": "path",
            "url": "../core"
        }
    ],
    "require-dev": {
        "phpunit/phpunit": "^4.3",
        "phpunit/DbUnit": "1.3.1",
        "codeception/codeception": "^2.0.9"
    },
    "suggest": {
        "ext-apc": "Allows for faster performance of Core Framework through APC caching",
        "ext-opcache": "Allows for faster performance of Core Framework through opCache(Zend Optimizer+) caching. opCache requires php 5.5+"
    },
    "authors": [
        {
            "name": "Shalom Sam",
            "email": "shalom.s@coreframework.in"
        }
    ],
    "scripts": {
        "post-install-cmd": [

        ],
        "post-update-cmd": [

        ]
    }
}
